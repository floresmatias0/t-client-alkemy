/**
 * @jest-environment jsdom
 */

describe('Main Page', () => {
    it('should render the container with the i18n props given and redirect on every click', () => {
        // eslint-disable-next-line no-console
        console.log('hola test');
    });
});
